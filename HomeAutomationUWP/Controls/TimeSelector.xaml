<UserControl
    x:Class="HomeAutomationUWP.Controls.TimeSelector"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HomeAutomationUWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:HomeAutomationUWP.Converters"
    xmlns:iconPacks="using:MahApps.Metro.IconPacks"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    >
    <UserControl.Resources>
        <converters:UShortToString x:Name="UShortToString"/>
    </UserControl.Resources>
    
    <StackPanel Orientation="Horizontal" x:Name="stackPanel">
        <TextBlock Text="Od" VerticalAlignment="Center" Margin="5"/>
        <TextBox x:Name="FromText" VerticalAlignment="Center" Margin="0,0,5,0" IsReadOnly="True" Text="{Binding From, Converter={StaticResource UShortToString}}"/>
        <Button x:Name="fromIncrease" Content="+" Click="ChangeTime" VerticalAlignment="Center" Margin="0,0,2,0"/>
        <Button x:Name="fromDecrease" Content="-" VerticalAlignment="Center" Margin="0,0,5,0"/>
        <TextBlock VerticalAlignment="Center" Margin="0,0,5,0" Text="do"/>
        <TextBox x:Name="ToText" VerticalAlignment="Center" Margin="0,0,5,0" IsReadOnly="True" Text="{Binding To, Converter={StaticResource UShortToString}}"/>
        <Button x:Name="toIncrease" Content="+" Click="ChangeTime" VerticalAlignment="Center" Margin="0,0,2,0"/>
        <Button x:Name="toDecrease" Content="-" Click="ChangeTime" VerticalAlignment="Center" Margin="0,0,5,0"/>
        <Button VerticalAlignment="Center" Margin="0,0,5,0" Command="{Binding DeleteEntry}" CommandParameter="{Binding CurrentCharacteristic}">
            <iconPacks:PackIconMaterial Kind="Delete"/>
        </Button>
    </StackPanel>
</UserControl>
